pipeline {
    agent any

    stages {
        stage('Running script') {
            steps {
                sh '''#!/bin/bash
                 echo "hello world" 
                '''
                
            }

        }
    }
}

